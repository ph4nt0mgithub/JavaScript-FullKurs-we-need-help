# Type Operators:

| Operators | Description                               | Example                                      |
|-----------|-------------------------------------------|----------------------------------------------|
|    typeof | Returns the type of a variable            | `let typeOfVar = typeof "hello"; // typeOfVar is "string"` |
|    instanceof| Checks if an object is of a specific type | `let isArray = [] instanceof Array; // isArray is true` |
